/// A favourite colour.
enum Colour {
  Red,
  Blue,
  Green
}

/// A piece of mail.
struct Mail {
  /// The destination address.
  address: String
}

/// Adds to numbers together.
fn add(a: i32, b: i32) -> i32 {
  a + b
}

fn main() {}